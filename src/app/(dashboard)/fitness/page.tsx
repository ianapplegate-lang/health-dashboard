import { TabStub } from "@/components/TabStub";

export default function FitnessPage() {
  return (
    <TabStub
      title="Fitness history"
      body="Running vs cycling pivot, monthly session breakdown by type, top runs table. Sourced from workouts table."
    />
  );
}
