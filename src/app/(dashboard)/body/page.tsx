import { TabStub } from "@/components/TabStub";

export default function BodyPage() {
  return (
    <TabStub
      title="Body composition"
      body="Weight scatter, body fat %, muscle mass, recomposition view. Queries the weight_samples table with sensor-anomaly filtering. Coming next."
    />
  );
}
