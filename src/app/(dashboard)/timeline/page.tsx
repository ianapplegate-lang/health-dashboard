import { TabStub } from "@/components/TabStub";

export default function TimelinePage() {
  return (
    <TabStub
      title="Timeline"
      body="Integrated chronological feed: Strava milestones, weight transitions, HRV/HR shifts, clinical events (biopsy, antiviral start). UNION across workouts/clinical/training tables."
    />
  );
}
