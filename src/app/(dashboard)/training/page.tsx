import { TabStub } from "@/components/TabStub";

export default function TrainingPage() {
  return (
    <TabStub
      title="Training plan"
      body="Session A/B cards with movement detail, 4-week schedule table, scheduling rationale, Norfolk trip plan, Block 2 placeholder. From training_sessions (8 rows seeded)."
    />
  );
}
