export function TabStub({ title, body }: { title: string; body: string }) {
  return (
    <div className="cs">
      <div className="ct">{title}</div>
      <div className="csub">Coming soon</div>
      <div className="note">{body}</div>
    </div>
  );
}
